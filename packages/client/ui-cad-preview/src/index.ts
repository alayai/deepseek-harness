/**
 * CAD preview plugin, node half. Pure UI plugin: host-side apply is empty;
 * the browser half is discovered through the package.json dsh.client row.
 */

/** Host plugin body; no host-side behavior. */
export function apply(): void {}
